名字, 统计ID, 描述, iOS版本, Android版本, iOS Log, Android Log, 参数
<% for(let e of event) { %><% for(let stat of e.value) { %><%= stat.name %>, <%= e.statId %>, <%= stat.description || '' %>, <%= stat.version_iOS %>, <%= stat.version_Android %>, <%= stat.iOS_log %>, <%= stat.Android_log %>, <%= stat.params ? stat.params.join(', ') : '' %>, <%= stat.getFullSourceName() %>
<% } %><% } %>
